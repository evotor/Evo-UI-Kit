import {EvoInputIconDirective} from './evo-input-icon.directive';

describe('EvoInputIconDirective', () => {
    it('should create an instance', () => {
        const directive = new EvoInputIconDirective();
        expect(directive).toBeTruthy();
    });
});
