import {EvoBadgeIconDirective} from './evo-badge-icon.directive';

describe('EvoBadgeIconDirective', () => {
    it('should create an instance', () => {
        const directive = new EvoBadgeIconDirective();
        expect(directive).toBeTruthy();
    });
});
