import {DATE_SEPARATOR} from './date-separator';
import {DATE_TIME_SEPARATOR} from './date-time-separator';

/**
 * @see angular date pipe docs
 */
export const DATE_TIME_COMPOSE_FORMAT = `dd${DATE_SEPARATOR}MM${DATE_SEPARATOR}YYYY${DATE_TIME_SEPARATOR}HH:mm`;
